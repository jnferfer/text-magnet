import React from 'react';
import { Network, Edge, Node, Options, NetworkEvents, IdType } from 'vis-network';
import 'vis-network/styles/vis-network.css';
export type { Network, Edge, Node, Options, NetworkEvents, IdType, };
export declare type GraphEvents = Partial<Record<NetworkEvents, (params?: any) => void>>;
export interface GraphData {
    nodes: Node[];
    edges: Edge[];
}
export interface NetworkGraphProps {
    graph: GraphData;
    options?: Options;
    getNetwork?: (network: any) => void;
    events?: GraphEvents;
    style?: React.CSSProperties;
    className?: string;
}
declare const VisGraph: React.ForwardRefExoticComponent<NetworkGraphProps & React.HTMLAttributes<HTMLDivElement> & React.RefAttributes<Network | undefined>>;
export default VisGraph;
